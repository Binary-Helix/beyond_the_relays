#Note from Puck - Copied events from Vanilla as they're currently commented out

namespace = btr_prim

# Space Station (HIDDEN)
country_event = {
	id = btr_prim.18
	hide_window = yes

	trigger = {
		is_country_type = primitive
		has_country_flag = early_space_age
		exists = home_planet
		home_planet = {
			has_observation_outpost = yes
		}
		NOT = {
			any_controlled_ship = {
				is_ship_size = primitive_space_station
			}
		}
	}

	mean_time_to_happen = {
		years = 60

		modifier = {
			factor = 0.6
			has_country_flag = acquired_tech
		}
	}

	immediate = {
		home_planet = {
			create_fleet = {
				name = "NAME_Space_Station"
				effect = {
					set_owner = ROOT
					create_ship = {
						name = "NAME_Space_Station"
						design = "NAME_Space_Station"
					}
					set_location = PREV
				}
			}
			if = {
				limit = {
					exists = observation_outpost_owner
				}
				observation_outpost_owner = {
					country_event = { id = btr_prim.19 }
				}
			}
		}
	}
}

# Space Station
country_event = {
	id = btr_prim.19
	title = "preftl.19.name"
	desc = "preftl.19.desc"
	picture = GFX_evt_satellite_in_orbit
	location = FROM

	is_triggered_only = yes

	option = {
		name = preftl.19.a
	}
}
